/**
 * Represents a Prisma Access Tenant with properties as PrismaAccessTenant
 */
export interface PrismaAccessTenant {
    tenant_name: string;
    client_id: string;
    client_secret: string;
    tsgId: string;
}
